UserDAO:
    SELECT : 
        user : userId -> User
            |
            +-> Inclu la récupération des boards associées

        lastId : -> int
        userBords : userID -> BoardId[]

    INSERT :
        user : email, username, password, creationDate, ip

    UPDATE :
        addIp : ip

BoardDAO:
    SELECT :
        board : id -> Board
            |
            +-> Inclu la récupération des boxs associées

        lastId : -> int
        rangeOfBoard : firstId, lastId -> Boards[]

    INSERT :
        board : name, description, gameMode, templateType, creationDate, finished, rate

    UPDATE :
        setFinished : finished
        updateRate : rate

BoxDAO:
    SELECT:
        box : id -> Box
            |
            +-> Inclu la récupération de la board associée
    
    INSERT :
        box : imgURL, drawable, drew, creationDate, width, height

    UPDATE :
        setImgURL : boxId, imgURL
        setDrawable : boxId, drawable
        setDrew : boxId, drew
